/**
 * Service Worker for Mosques in Oman
 * Cache Strategy: stale-while-revalidate
 * Generated by PWA Converter v2.0.2
 */

'use strict';

const CACHE_NAME = 'mosquesui-v1';
const OFFLINE_URL = '/mosquesui/offline.html';
const URLS_TO_CACHE = [
  "/mosquesui/",
  "/mosquesui/index.html",
  "/mosquesui/manifest.json",
  "/mosquesui/offline.html"
];

self.addEventListener('install', event => {
    console.log('[Service Worker] Installing for Mosques in Oman...');
    event.waitUntil(
        caches.open(CACHE_NAME)
            .then(cache => {
                console.log('[Service Worker] Caching app shell and all files for Mosques in Oman');
                return cache.addAll(URLS_TO_CACHE);
            })
            .then(() => {
                console.log('[Service Worker] All files cached successfully for Mosques in Oman');
                return self.skipWaiting();
            })
            .catch(error => {
                console.error('[Service Worker] Caching failed for Mosques in Oman:', error);
            })
    );
});

self.addEventListener('activate', event => {
    console.log('[Service Worker] Activating for Mosques in Oman...');
    event.waitUntil(
        caches.keys().then(cacheNames => {
            return Promise.all(
                cacheNames
                    .filter(name => name !== CACHE_NAME)
                    .map(name => {
                        console.log('[Service Worker] Deleting old cache:', name);
                        return caches.delete(name);
                    })
            );
        }).then(() => self.clients.claim())
    );
});

self.addEventListener('fetch', event => {
// Stale While Revalidate Strategy
    if (event.request.method !== 'GET') {
        return;
    }

    event.respondWith(
        caches.match(event.request).then(cachedResponse => {
            const fetchPromise = fetch(event.request).then(response => {
                // Check if we received a valid response
                if (response && response.status === 200) {
                    const responseToCache = response.clone();
                    caches.open(CACHE_NAME).then(cache => {
                        cache.put(event.request, responseToCache);
                    });
                }
                return response;
            }).catch(error => {
                // If the network request fails, and it's a navigation request,
                // serve the offline page.
                console.error('[Service Worker] Fetch failed; returning offline page if navigation.', error);
                if (isNavigationRequest(event.request)) {
                    return caches.match(OFFLINE_URL);
                }
            });

            // Return the cached response if it exists, otherwise wait for the network
            return cachedResponse || fetchPromise;
        })
    );
});

function isNavigationRequest(request) {
    return request.mode === 'navigate' || 
           (request.method === 'GET' && request.headers.get('accept').includes('text/html'));
}

console.log('[Service Worker] Loaded successfully for Mosques in Oman');
/**
 * Service Worker for Hadith Explorer
 * Generated by PWA Converter v2.0.1
 * Cache Strategy: stale-while-revalidate
 * Folder: hadith
 * Total Files Cached: 915
 */

const CACHE_NAME = 'hadith-v4';
const OFFLINE_URL = '/my-pwa-apps/hadith/offline.html';

const urlsToCache = [
  "/my-pwa-apps/hadith/favicon.ico",
  "/my-pwa-apps/hadith/icons/icon-72x72.png", 
  "/my-pwa-apps/hadith/icons/icon-96x96.png", 
  "/my-pwa-apps/hadith/icons/icon-128x128.png", 
  "/my-pwa-apps/hadith/icons/icon-144x144.png", 
  "/my-pwa-apps/hadith/icons/icon-152x152.png", 
  "/my-pwa-apps/hadith/icons/icon-192x192.png", 
  "/my-pwa-apps/hadith/icons/icon-384x384.png", 
  "/my-pwa-apps/hadith/icons/icon-512x512.png",
  "/my-pwa-apps/hadith/",
  "/my-pwa-apps/hadith/index.html",
  "/my-pwa-apps/hadith/manifest.json",
  "/my-pwa-apps/hadith/books-chapters.json",
  "/my-pwa-apps/hadith/books.json",
  "/my-pwa-apps/hadith/about.html",
  "/my-pwa-apps/hadith/style.css",
  "/my-pwa-apps/hadith/chapter-range.json",
  "/my-pwa-apps/hadith/hadiths/tirmidhi/chapter-1.json",
  "/my-pwa-apps/hadith/hadiths/tirmidhi/chapter-2.json",
  "/my-pwa-apps/hadith/hadiths/tirmidhi/chapter-49.json",
  "/my-pwa-apps/hadith/hadiths/malik/chapter-1.json",
  "/my-pwa-apps/hadith/hadiths/malik/chapter-2.json",
  "/my-pwa-apps/hadith/hadiths/malik/chapter-61.json",
  "/my-pwa-apps/hadith/hadiths/nasai/chapter-1.json",
  "/my-pwa-apps/hadith/hadiths/nasai/chapter-2.json",
  "/my-pwa-apps/hadith/hadiths/nasai/chapter-3.json",
  "/my-pwa-apps/hadith/hadiths/muslim/chapter-1.json",
  "/my-pwa-apps/hadith/hadiths/muslim/chapter-2.json",
  "/my-pwa-apps/hadith/hadiths/muslim/chapter-0.json",
  "/my-pwa-apps/hadith/hadiths/ibnmajah/chapter-1.json",
  "/my-pwa-apps/hadith/hadiths/ibnmajah/chapter-2.json",
  "/my-pwa-apps/hadith/hadiths/ibnmajah/chapter-3.json",
  "/my-pwa-apps/hadith/hadiths/bukhari/chapter-1.json",
  "/my-pwa-apps/hadith/hadiths/bukhari/chapter-2.json",
  "/my-pwa-apps/hadith/hadiths/bukhari/chapter-96.json",
  "/my-pwa-apps/hadith/hadiths/darimi/chapter-1.json",
  "/my-pwa-apps/hadith/hadiths/darimi/chapter-2.json",
  "/my-pwa-apps/hadith/hadiths/darimi/chapter-23.json",
  "/my-pwa-apps/hadith/hadiths/ahmed/chapter-1.json",
  "/my-pwa-apps/hadith/hadiths/ahmed/chapter-2.json",
  "/my-pwa-apps/hadith/hadiths/ahmed/chapter-31.json",
  "/my-pwa-apps/hadith/hadiths/abudawud/chapter-1.json",
  "/my-pwa-apps/hadith/hadiths/abudawud/chapter-2.json",
  "/my-pwa-apps/hadith/hadiths/abudawud/chapter-3.json",
  "/my-pwa-apps/hadith/offline.html"
];

// Install event - cache ALL resources
self.addEventListener('install', event => {
    console.log('[Service Worker] Installing for Hadith Explorer...');
    console.log('[Service Worker] Caching 915 files...');
    event.waitUntil(
        caches.open(CACHE_NAME)
            .then(cache => {
                console.log('[Service Worker] Caching app shell and all files');
                return cache.addAll(urlsToCache);
            })
            .then(() => {
                console.log('[Service Worker] All files cached successfully');
                return self.skipWaiting();
            }).then(() => {
                // Request persistent storage to prevent cache eviction
                if (navigator.storage && navigator.storage.persist) {
                    navigator.storage.persist().then(granted => {
                        console.log('[Service Worker] Persistent storage granted:', granted);
                    });
                }
            })
            .catch(error => {
                console.error('[Service Worker] Cache failed:', error);
            })
    );
});

// Activate event - clean up old caches
self.addEventListener('activate', event => {
    console.log('[Service Worker] Activating...');
    event.waitUntil(
        caches.keys().then(cacheNames => {
            return Promise.all(
                cacheNames
                    .filter(name => {
                        const cachePrefix = CACHE_NAME.replace(/-v\d+$/, "");
                        return name.startsWith(cachePrefix + "-") && name !== CACHE_NAME;
                    })
                    .map(name => {
                        console.log('[Service Worker] Deleting old cache:', name);
                        return caches.delete(name);
                    })
            );
        }).then(() => self.clients.claim())
    );
});

// Fetch event - Optimized Cache-First Strategy
self.addEventListener('fetch', event => {
    if (event.request.method !== 'GET') return;

    // Handle Favicon
    if (event.request.url.includes('favicon.ico')) {
        event.respondWith(
            caches.match(event.request).then(response => {
                return response || fetch(event.request).catch(() => new Response(null, { status: 204 }));
            })
        );
        return;
    }

    // Main Strategy: Cache-First
    event.respondWith(
        caches.match(event.request).then(cachedResponse => {
            // If found in cache, return immediately
            if (cachedResponse) return cachedResponse;

            // Otherwise, get from network and add to cache
            return fetch(event.request).then(response => {
                if (response && response.status === 200) {
                    const responseToCache = response.clone();
                    caches.open(CACHE_NAME).then(cache => {
                        cache.put(event.request, responseToCache);
                    });
                }
                return response;
            }).catch(() => {
                // If offline and navigating, show offline page
                if (isNavigationRequest(event.request)) {
                    return caches.match(OFFLINE_URL);
                }
            });
        })
    );
});



// Background sync (if supported)
self.addEventListener('sync', event => {
    if (event.tag === 'sync-data') {
        event.waitUntil(syncData());
    }
});

async function syncData() {
    console.log('[Service Worker] Background sync triggered');
    // Implement your sync logic here
}

// Helper function to check if request is navigational
function isNavigationRequest(request) {
    return request.mode === 'navigate' || 
           (request.method === 'GET' && request.headers.get('accept').includes('text/html'));
}

console.log('[Service Worker] Loaded successfully for Hadith Explorer');
console.log('[Service Worker] Scope: /my-pwa-apps/hadith/');
console.log('[Service Worker] Caching 915 files including all your assets');
